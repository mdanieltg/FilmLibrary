@model IEnumerable<FilmLibrary.Domain.Entities.Country>
@{ ViewData["Title"] = "Países"; }

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Inicio</a></li>
        <li class="breadcrumb-item active">@ViewData["Title"]</li>
    </ol>
</nav>

<h2>@ViewData["Title"]</h2>

<div>
    <a asp-action="Create" class="btn btn-primary">Agregar país</a>
</div>

<table class="table table-striped">
    <thead>
    <tr>
        <th>Id</th>
        <th>Nombre</th>
        <th>Acciones</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var country in Model)
    {
        <tr>
            <td>@country.Id</td>
            <td>
                <a asp-action="Details" asp-route-countryId="@country.Id">@country.Name</a>
            </td>
            <td>
                <a asp-action="Edit" asp-route-countryId="@country.Id">Editar</a> |
                <a asp-action="Delete" asp-route-countryId="@country.Id">Eliminar</a>
            </td>
        </tr>
    }
    </tbody>
</table>
