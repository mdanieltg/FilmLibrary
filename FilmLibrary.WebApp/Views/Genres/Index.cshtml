@model IEnumerable<FilmLibrary.Domain.Entities.Genre>
@{ ViewData["Title"] = "Géneros"; }

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Inicio</a></li>
        <li class="breadcrumb-item active">@ViewData["Title"]</li>
    </ol>
</nav>

<h2>@ViewData["Title"]</h2>

<p>
    @Html.ActionLink("Crear", "Create")
</p>

<table class="table table-striped">
    <thead>
    <tr>
        <th>Id</th>
        <th>Nombre</th>
        <th>Acciones</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var director in Model)
    {
        <tr>
            <td>@director.Id</td>
            <td>@director.Name</td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { genreId = director.Id }) |
                @Html.ActionLink("Detalles", "Details", new { genreId = director.Id }) |
                @Html.ActionLink("Eliminar", "Delete", new { genreId = director.Id })
            </td>
        </tr>
    }
    </tbody>
</table>
